{% extends "university/edit_university.html" %}


{% block pathimage %}
  <img src ="/tuerasmus/media/{{path_image}}" width="300px" heigth="300px"/>
  <!--<form action="" enctype="multipart/form-data" method="post"> {% csrf_token %}
    {{form0.as_p}}
    <button class="btn btn-sm btn-primary" type="submit">Guardar cambios</button>
  </form>-->
{% endblock %}


{% block uni_form %}

  {% if not no_user %}
  
      <div class="page-header">
        <h1><a href="/tuerasmus/university/{{uni_name}}" style="color: #424242">{{ uniname }}</a></h1>
        <h6>Pincha sobre el nombre de la universidad para ir al perfil de la universidad</h6>
      </div>
      
      <form action="" enctype="multipart/form-data" method="post"> {% csrf_token %}
        {% if alerterror %}
          <br>
          <div class="alert alert-danger">
            <center><strong>¡¡ERROR!!</strong>, asegúrate te introducir bien los datos</center>
          </div>
        {% endif %}

        {% if alertdone %}
          <br>
          <div class="alert alert-success">
            <center><strong>BIEN!! <big>{{usu}}</big>, ¡la información de {{uniname}} quedó guardada correctamente!</strong></center>
          </div>
        {% endif %}
        
        {% if coor %}
          <br>
          <p>
            <h3>Para obtener las coordenadas pincha en el siguiente enlace:</h3>
            <a class="btn btn-lg btn-success" href="http://mygeoposition.com/" target="_blank">Coordenadas</a>
          </p>
        {% endif %}
        
        <h3><strong>{{tit}}</strong></h3>
        {{form.as_p}}
        <br><br>
        <h3><strong>{{tit1}}</strong></h3>
        {{form1.as_p}}
        <br><br>
        <h3><strong>{{tit2}}</strong></h3>
        {{form2.as_p}}
        <br><br>
        <h3><strong>{{tit3}}</strong></h3>
        {{form3.as_p}}
        <br><br>
        <h3><strong>{{tit4}}</strong></h3>
        {{form4.as_p}}
        <br><br>
        <h3><strong>{{tit5}}</strong></h3>
        {{form5.as_p}}
        
        {% if not saved %}
          <br><br>
          <button class="btn btn-primary" type="submit">Guardar</button>
        {% endif %}
        <br><br>
      </form>
      
  {% else %}
  
      <div class="page-header">
        <h1>El usuario {{user_infobasic}} ya registro estos datos de la universidad</h1>
      </div>
      <br>
      <h1>Por tanto, solo deberás rellenar los formularios de los distintos botones</h1>
      
  {% endif%}
  
{% endblock %}
